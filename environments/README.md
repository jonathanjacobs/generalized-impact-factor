# Environments

`requirements.txt` supports `impact_lookup.py` and its test suite (`tests/`). It has been checked with Python 3.9.19; no other Python version has been tested.

Create the environment with:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r environments/requirements.txt
```
